# AI联谊匹配系统 - 项目总结

## 项目概述

基于MBTI人格理论的AI智能联谊匹配系统，支持500人规模的两两最优配对。

## 核心功能

### 1. 问卷系统
- 20道MBTI人格测试题
- 4维度8类型人格分类
- 16种人格类型（ENFJ、ISTP等）
- 兴趣标签选择

### 2. 人格分析
- 实时计算人格类型
- 维度得分可视化
- 人格特质描述
- 最佳匹配类型推荐

### 3. 智能匹配
- **同好匹配**：相似人格优先配对
- **互补匹配**：差异人格优先配对
- **最优算法**：NetworkX最大权匹配
- **匹配分数**：综合人格相似度(40%) + 兴趣匹配(40%) + 类型组合(20%)

### 4. 管理后台
- 实时统计数据
- 参与者管理
- 一键执行匹配
- 匹配结果查看
- 数据导出支持

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 (Vue3 + Vite)                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 首页    │  │ 问卷    │  │ 结果    │  │ 管理    │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
└─────────────────────────────────────────────────────────────┘
                              ↓ REST API
┌─────────────────────────────────────────────────────────────┐
│                    后端 (Python + FastAPI)                   │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │ 问卷API │  │ 人格分析│  │ 匹配算法│  │ 统计API │        │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      数据 (SQLite)                           │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │ participants │  │ match_results│                        │
│  └──────────────┘  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

## 文件结构

```
match-making-app/
├── README.md                 # 项目说明
├── QUICKSTART.md            # 快速启动指南
├── BUILD_EXE.md             # 打包EXE指南
├── start.bat                # Windows启动脚本
├── start.sh                 # Linux/Mac启动脚本
├── .gitignore               # Git忽略文件
│
├── backend/                 # Python后端
│   ├── main.py             # FastAPI主程序
│   ├── database.py         # 数据库模型
│   ├── matching.py         # 匹配算法
│   └── requirements.txt    # Python依赖
│
└── frontend/               # Vue3前端
    ├── index.html          # HTML入口
    ├── package.json        # Node依赖
    ├── vite.config.js      # Vite配置
    └── src/
        ├── main.js         # 入口文件
        ├── App.vue         # 根组件
        ├── router/         # 路由配置
        └── views/          # 页面组件
            ├── HomeView.vue    # 首页
            ├── QuizView.vue    # 问卷页
            ├── ResultView.vue  # 结果页
            └── AdminView.vue   # 管理页
```

## 快速启动

### Windows用户

双击运行 `start.bat` 即可自动完成：
1. 安装后端依赖
2. 启动后端服务 (http://localhost:8000)
3. 安装前端依赖
4. 启动前端服务 (http://localhost:3000)
5. 自动打开浏览器

### 手动启动

```bash
# 1. 启动后端
cd backend
pip install -r requirements.txt
python main.py

# 2. 启动前端（新终端）
cd frontend
npm install
npm run dev

# 3. 访问 http://localhost:3000
```

## 使用流程

### 用户端

1. 访问首页，点击"开始问卷"
2. 填写基本信息（姓名、学号、性别）
3. 完成20道人格测试题
4. 选择兴趣标签
5. 提交后查看人格分析报告
6. 等待管理员执行匹配
7. 查看匹配结果

### 管理端

1. 访问管理后台
2. 查看报名人数和人格分布
3. 确认所有参与者已报名
4. 选择匹配模式（同好/互补）
5. 点击"开始AI匹配"
6. 查看匹配结果和匹配理由
7. 导出结果用于活动

## 匹配算法详解

### 人格维度

| 维度 | 类型A | 类型B | 说明 |
|------|-------|-------|------|
| E/I | 外向 | 内向 | 能量来源 |
| S/N | 实感 | 直觉 | 信息获取 |
| T/F | 思考 | 情感 | 决策方式 |
| J/P | 判断 | 感知 | 生活方式 |

### 相似度计算

```python
相似度 = 人格类型相似度 × 40% + 
         兴趣标签匹配度 × 40% + 
         最佳类型组合 × 20%
```

### 最优配对

使用NetworkX的`max_weight_matching`算法：
- 构建完全图，边权重为相似度
- 求最大权匹配
- 保证全局最优
- 每人只配一人

## 扩展建议

1. **用户认证** - 添加登录注册，保护隐私
2. **邮件通知** - 匹配完成自动发送邮件
3. **批量导入** - 支持Excel导入参与者
4. **实时推送** - WebSocket实时更新匹配结果
5. **数据导出** - 导出Excel/PDF报告
6. **移动端适配** - 优化手机端体验

## 许可证

MIT License - 可自由使用和修改

## 联系方式

如有问题或建议，欢迎反馈！
