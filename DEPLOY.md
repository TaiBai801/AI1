# AI联谊匹配系统 - 双版本部署说明

## 版本说明

本项目包含两个独立的部署版本：

### 1. 用户端 (`dist-user/`)
- **用途**: 供参与者填写问卷
- **访问路径**: http://localhost:3000
- **功能**: 首页、问卷填写、结果查看
- **特点**: 无管理后台入口，无敏感功能

### 2. 管理端 (`dist-admin/`)
- **用途**: 供组织者管理活动
- **访问路径**: http://localhost:3000/admin
- **密码**: qsxcyy33
- **功能**: 统计概览、参与者管理、AI匹配、结果查看

---

## 快速启动

### 方式一：一键启动（推荐）

双击运行 `start.bat`，会自动启动前后端服务。

然后访问：
- 用户端: http://localhost:3000
- 管理端: http://localhost:3000/admin (密码: qsxcyy33)

### 方式二：手动启动

```bash
# 1. 启动后端
cd backend
python main.py

# 2. 启动前端（新终端）
cd frontend
npm run dev
```

---

## 分离部署（生产环境）

### 用户端部署

```bash
cd frontend

# 构建用户端（移除管理后台路由）
npm run build

# 复制到用户端目录
copy dist\* ..\dist-user\
```

### 管理端部署

管理端使用相同的前端代码，只是通过密码保护访问 `/admin` 路径。

---

## 目录结构

```
match-making-app/
├── backend/              # Python后端（共用）
│   ├── main.py
│   ├── matching.py
│   └── ...
├── frontend/             # Vue3前端源码
│   ├── src/
│   └── ...
├── dist-user/            # 用户端部署文件
├── dist-admin/           # 管理端部署文件
├── start.bat             # Windows一键启动
└── README.md
```

---

## 安全说明

1. **密码保护**: 管理后台需要密码 (qsxcyy33) 才能访问
2. **路由隔离**: 普通用户无法通过导航菜单进入管理后台
3. **API权限**: 建议生产环境添加API认证

---

## 使用流程

### 组织者

1. 访问 http://localhost:3000/admin
2. 输入密码: qsxcyy33
3. 查看报名人数统计
4. 等待所有参与者完成问卷
5. 点击"开始AI匹配"
6. 查看匹配结果

### 参与者

1. 访问 http://localhost:3000
2. 点击"开始问卷"
3. 填写基本信息（姓名、学号、性别）
4. 完成20道MBTI测试题
5. 选择兴趣标签
6. 提交后查看人格分析
7. 等待组织者公布匹配结果

---

## 注意事项

- 后端服务必须始终运行（端口8000）
- 前端服务运行在端口3000
- 首次使用需要安装依赖（运行start.bat会自动安装）
